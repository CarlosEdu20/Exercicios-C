#include <stdio.h>

int main () {
    int i = 1;
    int num;
    float media = 0;

    while (i <= 10) {
        printf("Digite um numero: ");
        scanf("%d", &num);
        int soma = soma + num;
        media = soma / 10;

        i++;

    }
    printf("Media = %.2f", media);








    return 0;
}
